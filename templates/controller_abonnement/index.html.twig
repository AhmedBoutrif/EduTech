{% extends 'base.html.twig' %}

{% block title %}Abonnement index{% endblock %}

{% block main %}
    <h1>Abonnement index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Durée</th>
                <th>Prix</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for abonnement in abonnements %}
            <tr>
                <td>{{ abonnement.id }}</td>
                <td>{{ abonnement.nom }}</td>
                <td>{{ abonnement.duree }}</td>
                <td>{{ abonnement.prix }}</td>
                <td>{{ abonnement.description }}</td>
                <td>
                    <a href="{{ path('app_controller_abonnement_show', {'id': abonnement.id}) }}">show</a>
                    <a href="{{ path('app_controller_abonnement_edit', {'id': abonnement.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_controller_abonnement_new') }}">Create new</a>
{% endblock %}
