<form method="post" action="{{ path('app_Avaibility_delete', {'id': Avaibility.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ Avaibility.id) }}">
    <button class="btn">Delete</button>
</form>
