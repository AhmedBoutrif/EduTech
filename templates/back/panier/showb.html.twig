{% extends 'base.html.twig' %}

{% block title %}Panier{% endblock %}

{% block main %}
    <h1>Panier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ panier.id }}</td>
            </tr>
            <tr>
                <th>IdPanier</th>
                <td>{{ panier.idPanier }}</td>
            </tr>
            <tr>
                <th>Nb_total</th>
                <td>{{ panier.nbTotal }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_controller_panier_index') }}">back to list</a>

    <a href="{{ path('app_controller_panier_edit', {'id': panier.id}) }}">edit</a>

    {{ include('controller_panier/_delete_form.html.twig') }}
{% endblock %}
